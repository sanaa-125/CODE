#include<stdio.h>
#include<stdlib.h>

int main(void)
{
    /* code */
    int a = 5;
    int b = 10;
    int sum = a + b;
    int produit = a * b;
    int modulo = a % b;
    printf (" voici le produit : %d\n", produit);
    printf (" voici la somme : %d\n", sum);
    printf (" voici le modulo : %d\n", modulo);

    return 0;
}
